<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.paulacr.acessibilidade" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity_" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LabelingActivity_"/>
        <activity android:name=".EditTextActivity_"/>
        <activity android:name=".LiveRegionActivity_"/>
        <activity android:name=".AgruparViewsActivity_"/>
        <activity android:name=".CustomViewActivity_"/>
        <activity android:name=".FloatingActionButtonActivity_"/>
        <activity android:name=".ViewsExpansiveisActivity_"/>
    </application>

</manifest>